- name: nginx install and run
  hosts: web
  become: yes # take sudo access
  tasks:
   - name: install nginx
     ansible.bultin.package: # heterogeneous module that can work for all destros
      name: nginx
      state: present